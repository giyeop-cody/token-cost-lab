# 실측 기록 — Gemini API 실호출 (exp08)

측정일: **2026-08-15** · 도구: `experiments/exp08_gemini_live.py`
이 문서의 숫자는 **시뮬레이션이 아니라 실제 API 응답의 `usageMetadata` 값**입니다.

---

## 0. 환경 메모 (재현하려는 사람을 위한 함정)

- **`gemini-2.5-*` 계열은 신규 API 키에서 404**가 납니다.
  `"This model is no longer available to new users"` — 모델 목록(`ListModels`)에는
  여전히 보이지만 실제 호출은 거부됩니다. **목록에 있다 ≠ 쓸 수 있다.**
  → `python experiments/exp08_gemini_live.py --list-models` 로 실제 가용 모델을 확인하세요.
- 사용 가능했던 모델: `gemini-flash-latest`, `gemini-pro-latest`, `gemini-3-flash-preview`,
  `gemini-3.1-pro-preview`, `gemini-3.1-flash-lite`, `gemini-3.5-flash`, `gemini-3.6-flash`,
  `gemini-3.7-flash`
- 측정 중 **429 / 500 / 503이 빈번**했습니다. 발표장 라이브 시연은 실패할 수 있으니
  **백업 캡처를 반드시 준비**하세요.
- 단가는 `lab/pricing.py` 의 `gemini-25`($1.25/$10 per 1M) 기준으로 환산했습니다.
  실제 사용 모델(flash 계열)의 공식 단가와는 다르므로, **비율만** 보세요.

---

## 1. 사고 토큰 = 요금 (★ 발표 하이라이트)

`gemini-3.1-flash-lite` · 동일 질문 · 사고 예산만 변경

| 사고 예산 | 사고 tok | 응답 tok | 출력계 | 비용 | 배수 |
|---|---:|---:|---:|---:|---:|
| 사고 끔 (0) | 0 | 4 | 4 | $0.000105 | 1× |
| 제한 512 | 144 | 5 | 149 | $0.001555 | **14.8×** |
| 자동 (−1) | 382 | 5 | 387 | $0.003935 | **37.5×** |

**해석 — 이게 덱의 핵심 주장 그 자체입니다.**

사용자가 받는 답은 4~5 토큰으로 **사실상 똑같습니다.** 그런데 비용은 **37배** 차이납니다.
차액 전부가 `thoughtsTokenCount`, 즉 **응답 어디에도 표시되지 않는 사고 토큰**입니다.

> "시키지도 않은 시공비"가 API 필드로 실재한다는 증거.
> 청구서에는 찍히는데 화면에는 안 보입니다.

라이브 시연 명령:
```bash
python experiments/exp08_gemini_live.py --thinking --model gemini-3.1-flash-lite
```

---

## 2. 한국어 vs 영어 — 입력 토큰 (countTokens, 무과금)

`gemini-flash-latest`

| 언어 | 글자 수 | 입력 토큰 | 배수 |
|---|---:|---:|---:|
| 영어 | 177 | 52 | 1.00× |
| 한국어 | 108 | 70 | **1.35×** |

- 글자 수는 한국어가 **더 적은데**(108 < 177) 토큰은 **더 많습니다**(70 > 52).
- exp01의 tiktoken 실측(o200k 1.44×)과 같은 방향, 조금 완만한 값.
- **결론: 배수는 토크나이저마다 다르다. 자기 모델로 재라.**

---

## 3. 한국어 vs 영어 — 전체 비용 (설명형 과제, 표본 5)

프롬프트: "프롬프트 캐싱이 무엇이고 언제 비용을 절감하는지 약 120단어로 설명하라" (동일 의미 한/영)

| 언어 | 입력 | 사고 | 응답 | 출력계 | 1회 비용 |
|---|---:|---:|---:|---:|---:|
| 영어 | 22 | 572 | 94 | 666 | $0.006687 |
| 한국어 | 33 | 767 | 29 | 796 | $0.008001 |

**한국어 +19.6%** · 입력 1.50× · 사고 1.34×

⚠️ **이 수치의 한계 (정직하게 밝힙니다)**
- 폴백 때문에 모델이 섞였습니다(`flash-latest`/`3.5-flash`/`3.1-flash-lite`).
- `maxOutputTokens=800`에 걸려 한국어 응답이 잘렸습니다(767+29≈800).
  즉 **한국어는 답을 다 못 쓰고도 영어보다 20% 비쌌습니다.** 실제 격차는 이보다 큽니다.
- 표본 5개. 통계적으로 유의하다고 말하기엔 부족합니다.

---

## 4. 예상과 달랐던 결과 — 반드시 함께 말할 것

첫 시도(짧은 계산 문제, 정답이 숫자 하나)에서는 **한국어가 0.5% 더 쌌습니다.**

| 언어 | 입력 | 사고 | 응답 | 비용 |
|---|---:|---:|---:|---:|
| 영어 | 52 | 337 | 1 | $0.003445 |
| 한국어 | 70 | 333 | 1 | $0.003427 |

**왜 뒤집혔나:**
1. 답이 `"1"` 한 토큰 → **출력에서 벌어질 여지가 없었다.**
2. 모델이 한국어 질문을 받고도 **내부적으로는 영어로 사고**했다 (사고 토큰 337 vs 333, 거의 동일).

**시사점 — 이게 오히려 원칙 ①의 정확한 형태입니다.**

> 한국어가 비싼 건 "한국어로 물어봐서"가 아니라 **"한국어로 길게 출력해서"** 입니다.
> 짧은 답을 뽑는 작업이라면 질문 언어는 비용에 거의 영향이 없습니다.
> **입력 언어가 아니라 출력·사고 언어와 길이가 요금을 결정합니다.**

발표에서 이 사례를 넣으면 "유리한 숫자만 골랐다"는 반론을 미리 막을 수 있습니다.

---

## 5. 검증된 API 동작

- `thoughtsTokenCount`가 별도 필드로 실재하며, 사고 예산에 비례해 증가함을 확인.
- `promptTokenCount`는 캐시분을 포함하므로 실제 과금 입력은 `prompt − cached`.
- `finishReason`이 `MAX_TOKENS`면 사고 토큰이 예산을 먹고 응답이 잘립니다.
  **사고 예산을 안 걸면 답이 안 나오고 요금만 나갈 수 있습니다.**

---

## 재현

```bash
export GEMINI_API_KEY="..."
python experiments/exp08_gemini_live.py --list-models   # 가용 모델 확인 (무과금)
python experiments/exp08_gemini_live.py --count-only    # 한/영 입력 배수 (무과금)
python experiments/exp08_gemini_live.py --thinking --model gemini-3.1-flash-lite
```

---

## §6. 표본 확대 재측정 (N=100) — 그리고 결론이 갈렸다

`tools/live_lang_bench.py --n 25` · `gemini-3.1-flash-lite` · 2026-08-15 · 100/100 성공(절단 0건)

### 6-1. 호출당 비용만 보면 결론이 뒤집힌다

| 과제 | 언어 | 입력 | 응답tok | 비용/호출 | 한국어 추가비용 |
|---|---|---:|---:|---:|---:|
| explain (설명형) | EN | 22 | 157 | $0.001595 | — |
| explain | KO | 33 | 203 | $0.002068 | **+29.7%** |
| reason (추론형) | EN | 63 | 367 | $0.003744 | — |
| reason | KO | 88 | 285 | $0.002963 | **−20.9%** |

N=5 예비측정의 +19.6%는 **재현되지 않았다.** explain은 +29.7%로 더 커졌고,
reason은 오히려 한국어가 **21% 싸게** 나왔다.

### 6-2. 왜 갈렸나 — 답변 분량을 안 맞췄기 때문

| 과제 | 언어 | 평균 글자수 | 토큰/글자 |
|---|---|---:|---:|
| explain | EN | 825 | 0.190 |
| explain | KO | 410 (**EN의 50%**) | 0.494 (**2.60×**) |
| reason | EN | 1,040 | 0.356 |
| reason | KO | 414 (**EN의 40%**) | 0.689 (**1.94×**) |

같은 프롬프트를 줬는데 **모델이 한국어로는 답을 절반도 안 썼다.**
reason에서 한국어가 싸 보인 건 언어가 효율적이어서가 아니라 **일을 덜 했기 때문**이다.

### 6-3. 분량을 정규화하면 결론은 그대로다

| 과제 | $/1,000자 EN | $/1,000자 KO | 배수 |
|---|---:|---:|---:|
| explain | $0.00193 | $0.00504 | **2.61×** |
| reason | $0.00360 | $0.00715 | **1.99×** |

**같은 양의 내용을 전달하는 데 드는 비용은 한국어가 영어의 약 2~2.6배다.**
exp01의 토크나이저 실측(o200k 1.44× / cl100k 2.22×)과 같은 구간이다.

### 6-4. 교훈 — 이 실험이 남긴 진짜 결론

1. **호출당 비용 비교는 함정이다.** 출력 길이를 통제하지 않으면 "덜 쓴 쪽이 싸다"는
   당연한 결과가 언어 효율로 오독된다. 반드시 **단위 분량당 비용**으로 정규화할 것.
2. **N=5는 결론을 내기에 부족했다.** 예비측정 +19.6%는 N=100에서 재현되지 않았다.
   (참고: reason/EN 응답토큰 표준편차 ±63 — 표본 5개로는 노이즈에 묻힌다.)
3. 그럼에도 **원래 주장의 방향은 유지된다.** 정규화 기준 2~2.6배.
   단 근거는 "한국어 호출이 비싸다"가 아니라 **"한국어는 같은 내용에 토큰을 2배 이상 쓴다"**여야 한다.
4. `thoughtsTokenCount`는 이 두 과제에서 **전 표본 0**이었다. 사고 토큰은 모델·과제·예산
   설정에 따라 켜지고 꺼진다(§1의 37배는 `thinkingConfig`를 명시했을 때). **기본값을 가정하지 말 것.**

### 재현

```bash
python tools/live_lang_bench.py --n 25 --model gemini-3.1-flash-lite
python tools/live_lang_bench.py --summarize out/live_lang.jsonl   # 재측정 없이 요약만
```

원자료: `out/live_lang.jsonl` (100행, 호출별 usageMetadata 원본)

> ⚠️ **이 §6의 원자료 파일은 배포 패키지에 포함되어 있지 않습니다.** 포함된 원자료는
> §7·§8의 `results/live_lang_thinking.jsonl`(80행)과 `results/thinking_sweep.jsonl`(30행)뿐입니다.
> §6 숫자를 검증하려면 위 명령으로 재측정해야 합니다(과금 발생).

---

## §7. 사고 예산을 켜고 재측정 (N=80) + 통계 검정

§6은 `thinkingConfig` 미지정 상태였다(사고 토큰 전 표본 0). 사고를 켜면 결론이
달라지는지 확인하기 위해 `--thinking -1`(자동)로 80건을 재측정했다.
`gemini-3.1-flash-lite` · 2026-08-15 · 80/80 성공.

### 7-1. 사고를 켜도 §6의 패턴은 그대로 재현된다

| 과제 | 언어 | 입력 | 사고 | 응답 | 비용/호출 | 호출당 차이 |
|---|---|---:|---:|---:|---:|---:|
| explain | EN | 22 | 697 | 144 | $0.008441 | — |
| explain | KO | 33 | 967 | 181 | $0.011515 | **+36.4%** |
| reason | EN | 63 | 1,616 | 214 | $0.018374 | — |
| reason | KO | 88 | 1,116 | 210 | $0.013375 | **−27.2%** |

설명형 +36.4% / 추론형 −27.2%. **부호가 갈리는 현상이 사고를 켠 뒤에도 동일하게
나타났다.** 즉 §6의 역전은 우연이 아니라 구조적인 것이다.

### 7-2. 원인도 동일하다 — 한국어가 답을 덜 쓴다

| 과제 | EN 글자 | KO 글자 | KO/EN |
|---|---:|---:|---:|
| explain | 822자 | 366자 | **0.45×** |
| reason | 570자 | 305자 | **0.54×** |

### 7-3. 분량 정규화 + 통계 검정 — 여기서 결론이 확정된다

| 과제 | $/1,000자 EN | $/1,000자 KO | 비율 | 95% 신뢰구간 | p값 | Cliff's δ |
|---|---:|---:|---:|---:|---:|---:|
| explain | $0.01029 | $0.03116 | **3.03×** | [2.68, 3.47] | ~0 | +1.000 (큼) |
| reason | $0.03246 | $0.04419 | **1.36×** | [1.23, 1.51] | 6.1e-10 | +0.835 (큼) |

**두 과제 모두 신뢰구간이 1.0을 포함하지 않는다.** 호출당 비용에서는 부호가
갈렸지만, **분량을 통제하면 한국어가 유의하게 비싸다**는 결론은 예외 없이 성립한다.
효과크기도 explain에서 δ=1.000 — 두 분포가 전혀 겹치지 않는다.

### 7-4. 사고 토큰도 언어에 따라 다르다

| 과제 | EN 사고 | KO 사고 | 비율 | 95% CI |
|---|---:|---:|---:|---:|
| explain | 697 | 967 | 1.39× | [1.18, 1.66] |
| reason | 1,616 | 1,116 | **0.69×** | [0.60, 0.80] |

추론형에서 **영어가 사고를 1.4배 더 많이 한다.** 영어로 더 깊이 생각하고 더 길게
답한 것이고, 이것이 §6·§7-1에서 추론형 한국어가 싸 보인 진짜 이유다.
**싼 게 아니라 덜 생각하고 덜 쓴 것이다.**

출력 토큰 중 사고 비중은 **83~88%** — 요금의 대부분은 사용자가 볼 수 없는 부분이다.

---

## §8. 사고 예산 스윕 — 같은 답, 240배 요금

`tools/thinking_sweep.py --n 6` · 질문·모델 고정 · `thinkingBudget`만 변경.
⚠️ 비용은 `gemini-25`($1.25/$10) 단가로 환산한 값이다. 실제 사용 모델(flash-lite)의
공식 단가로 계산하면 배수는 **약 149배**가 된다. 240배는 단가 가정에 의존하는 값이므로
발표 시 "이 단가 기준"임을 밝힐 것.
최종 답변만 요구하는 계산 문제(정답 $38.016)를 30회 호출했다.

| 사고 예산 | n | 사고 토큰 | 응답 토큰 | 비용/호출 | 배수 | 정답률 (±0.05) |
|---|---:|---:|---:|---:|---:|---|
| 미지정(기본) | 6 | 0 | 6 | $0.000155 | 1.0× | **6/6** |
| 끔 (0) | 6 | 0 | 6 | $0.000158 | 1.0× | 5/6 (1회 `380.16` 자릿수 오류) |
| 512 | 6 | 142 | 6 | $0.001570 | **10.1×** | **2/6** (37.32 / 40.13 / 42.24 / 40.32) |
| 2048 | 6 | 1,114 | 6 | $0.011302 | **72.9×** | 6/6 (4회 `38.016`, 2회 `37.9944`) |
| 자동 (−1) | 6 | 3,704 | 6 | $0.037203 | **240.0×** | 6/6 (3회 `38.016`, 3회 `37.9944`) |

**사고를 끄든 자동으로 켜든 정답률은 6/6으로 같았다. 요금만 240배 차이났다.**
사고가 켜진 구간에서 출력 토큰의 **99%가 보이지 않는 사고 토큰**이다(실측 99.6%).

> 📊 **정답률의 신뢰구간(n=6).** Wilson 95% CI로 보면 512는 2/6 [10–70%],
> 나머지는 6/6 [61–100%]로 **구간이 겹친다.** 표본이 작아 "512가 유의하게 나쁘다"고
> 단정할 수 없다. 경향으로만 말할 것. 반면 **비용 배수는 분산이 거의 없어 견고하다.**

> ⚠️ **초기 원고의 오기 정정.** 이전 판은 "답변은 전부 `38.016`으로 동일"이라고 적었으나,
> 원자료를 재집계하면 **응답 문자열은 동일하지 않다.** `38.016` / `$38.016` / `37.9944`가 섞여 있고,
> 끔(0) 조건에서는 `380.16`(10배 오류)이 1회 나왔다. `37.9944`는 1,759회분으로 계산한 값(off-by-one)이다.
> 논지는 그대로 유지된다 — **정답률은 동등한데 요금만 240배**. 다만 "글자까지 똑같았다"는 표현은
> 사실과 다르므로 발표에서 쓰지 말 것.

주목할 점 두 가지:
1. **예산 512는 정답률이 2/6으로 떨어졌다** (37.32 / 40.13 / 42.24 / 40.32 오답). 사고를 어중간하게 주면 추론을 시작했다가
   끊겨서 품질이 더 나빠질 수 있다. 예산은 충분히 주거나 아예 끄는 편이 낫다.
2. **끔(0) 조건에서도 자릿수 오류가 1회 나왔다.** 사고를 끄면 항상 안전한 것이 아니라,
   이 문제 난이도에서 사고 유무가 정답률에 유의한 차이를 만들지 않았다는 뜻이다(n=6, 검정력 낮음).
3. **이 모델의 기본값은 사고 꺼짐이었다.** 하지만 이는 모델마다 다르다.
   `thinkingBudget`을 명시하지 않으면 어느 쪽으로 과금될지 알 수 없다 —
   **반드시 명시할 것.**

### 재현

```bash
export GEMINI_API_KEY=...
python tools/live_lang_bench.py --n 20 --thinking -1 --out results/live_lang_thinking.jsonl
python tools/stats_test.py results/live_lang_thinking.jsonl
python tools/thinking_sweep.py --n 6
```

원자료: `results/live_lang_thinking.jsonl`(80행) · `results/thinking_sweep.jsonl`(30행)
검정 출력: `results/stats_thinking.txt` · `results/sweep_summary.txt`
